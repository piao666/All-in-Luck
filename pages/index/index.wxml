<view class="container">
  <view class="title">{{welcome}}</view>
  <view class="subtitle">休闲竞技 · 社交娱乐</view>

  <view class="section">
    <view class="section-title">服务内容</view>
    <block wx:for="{{services}}" wx:key="*this">
      <view class="item">• {{item}}</view>
    </block>
  </view>
</view>
